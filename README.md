# DAO Governor



Install dependent environment

```shell
npm install
```

# Testing

run Hardhat Network
```shell
npx hardhat node
```


run the test suite:
```shell
npx hardhat test --network localhost
```
